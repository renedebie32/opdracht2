name: app docker container

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker images
 
      - name: Run a multi-line script
        run: |
          cd ~/actions-runner/_work/opdracht2/opdracht2/containers/     
          sudo docker compose up -d
